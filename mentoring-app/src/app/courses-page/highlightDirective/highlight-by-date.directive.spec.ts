import { HighlightByDateDirective } from './highlight-by-date.directive';

describe('HighlightByDateDirective', () => {
  it('should create an instance', () => {
    // const directive = new HighlightByDateDirective();
    // expect(directive).toBeTruthy();
  });
});
