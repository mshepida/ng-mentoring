export interface Course {
    id: number,
    title: string,
    creation_date: string,
    duration: number,
    description: string
  }
  
 export interface User {
    id: number,
    first_name: string,
    last_name: string
  }